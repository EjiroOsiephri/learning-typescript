"use strict";
let age;
age = 30;
const userName = "maximian";
console.log(userName);
// im the cofig json file we can creat two sub folders dist and src and we can tell typescript where we want our
//compiles files to be
// outdir is where output files are stored
// root dir is thelocation for the storage of input files
// strict type checkig options
function checkHandles(message) {
    console.log(message);
}
checkHandles("ejiro has 2020 goals");
// no implicit type function will not work if the type set on aparameter is the any type
//# sourceMappingURL=app.js.map